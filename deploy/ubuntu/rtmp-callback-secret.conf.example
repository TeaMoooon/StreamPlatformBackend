# Install to /etc/nginx/snippets/rtmp-callback-secret.conf (chmod 640 root:www-data).
# Value must match Rtmp:Secret in appsettings.Local.json.
# Generate via: sudo bash deploy/ubuntu/sync-rtmp-secret-from-local.sh
set $rtmp_callback_secret "CHANGE_ME_MATCH_NGINX_NOTIFY_SECRET";
